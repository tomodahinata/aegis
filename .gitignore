node_modules/
dist/
.turbo/
coverage/
*.tsbuildinfo
.next/
out/
.DS_Store
*.log
npm-debug.log*

# Secrets never belong in git (the toolkit itself enforces this elsewhere).
.env
.env.*
!.env.example

# Per-user Claude Code config (machine-local; shared config goes in .claude/settings.json).
.claude/settings.local.json
