node_modules/
dist/
.turbo/
coverage/
*.tsbuildinfo
.next/
out/
.DS_Store
*.log
npm-debug.log*

# Secrets never belong in git (the toolkit itself enforces this elsewhere).
.env
.env.*
!.env.example

# Per-user Claude Code config (machine-local; shared config goes in .claude/settings.json).
.claude/settings.local.json

# Commercialization / VC strategy memos — local reference ONLY. This is a PUBLIC
# repo (origin = github.com/tomodahinata/aegis); these confidential docs must
# never be committed here. The authoritative copies live in the private
# `aegis-dev` repo, the `aegis-pre-public-backup` tag, and ~/aegis-private-docs/.
docs/commercialization/
