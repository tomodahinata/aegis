# Pin the public registry so a developer's custom default can't silently redirect installs or publishes.
# Publish auth (//registry.npmjs.org/:_authToken) is injected at release time by the Release workflow
# from the NPM_TOKEN secret — never committed — so local `pnpm install` of these public packages is
# unaffected.
registry=https://registry.npmjs.org/
